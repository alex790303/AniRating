# AniRating 操作手冊

這份手冊假設你完全沒有技術背景。照著做就好，不需要理解程式碼。

---

## 這些檔案是什麼

| 檔案 | 作用 | 你需要動它嗎 |
|---|---|---|
| `scripts/config.mjs` | 設定：抓幾年、各站權重 | 想調整時才改 |
| `scripts/sources.mjs` | 跟五個評分站要資料 | 不用 |
| `scripts/build.mjs` | 計算綜合分、產生資料檔 | 不用 |
| `.github/workflows/update.yml` | 每天自動執行的排程 | 不用 |
| `data/` | 程式產生的資料，網站讀這裡 | 不用（自動生成） |

---

## 階段 3：註冊帳號

### 3-1　GitHub（存放程式與資料）

1. 開 https://github.com/signup
2. 用你的 email 註冊，選免費方案（Free）
3. 註冊完成後，點右上角「+」→「New repository」
4. Repository name 填 `anirating`
5. 選 **Public**（公開。選 Private 的話 Actions 每月只有 2000 分鐘額度，Public 無限）
6. 按「Create repository」

> **為什麼要 Public**：資料本來就是要給大家看的，而且免費額度無上限。你的 API 金鑰不會放在程式碼裡，是存在另一個加密的地方，不會外洩。

### 3-2　Cloudflare（放網站）

1. 開 https://dash.cloudflare.com/sign-up
2. 用 email 註冊，全部選免費方案
3. 先停在這裡，等程式上傳完再回來設定

---

## 階段 4：申請 API 金鑰

只有 IMDb 需要金鑰，其他四站都不用。

### OMDb（用來取得 IMDb 評分）

1. 開 https://www.omdbapi.com/apikey.aspx
2. 選 **FREE! (1,000 daily limit)**
3. 填 email 和用途（用途可以寫 `anime rating aggregation site`）
4. 送出後收信，信裡有一串金鑰，長得像 `a1b2c3d4`
5. **點信中的啟用連結**，不點的話金鑰不會生效
6. 把金鑰記下來，下一步要用

> 每天 1000 次查詢的限制，剛好夠更新最近三季。第一次抓全部五年的資料時，IMDb 會抓不完，程式會自動跳過抓不到的部分，隔天排程再補。這是正常的。

---

## 階段 5：上傳與上線

### 5-1　把檔案放進 GitHub

1. 進入你剛建立的 `anirating` 倉庫頁面
2. 點「uploading an existing file」
3. 把我給你的整個資料夾拖進去
4. 下方按「Commit changes」

### 5-2　設定 API 金鑰（重要：不要直接寫進程式碼）

1. 在倉庫頁面點上方「Settings」
2. 左側選「Secrets and variables」→「Actions」
3. 按「New repository secret」
4. Name 填 `OMDB_API_KEY`，Secret 貼上你的金鑰
5. 按「Add secret」

### 5-3　第一次抓資料

1. 倉庫頁面點上方「Actions」
2. 左側點「更新評分資料」
3. 右側「Run workflow」→ 把 `recent` 改成 `full` → 綠色按鈕執行
4. **這次會跑很久（約 2–4 小時）**，因為要抓五年份、又要遵守各站的請求速率限制。你可以關掉網頁去做別的事，它在雲端跑。
5. 跑完後倉庫裡會出現 `data` 資料夾

之後每天凌晨 3 點會自動只更新最近三季，大約 10 分鐘跑完，不用你管。

### 5-4　把網站掛上去

1. 回到 Cloudflare 後台，左側選「Workers & Pages」
2. 「Create」→「Pages」→「Connect to Git」
3. 授權 GitHub，選 `anirating` 倉庫
4. 建置設定全部留空（因為是純靜態網站，不需要建置）
5. 按「Save and Deploy」
6. 約一分鐘後會給你網址：`anirating.pages.dev`

之後每次資料更新，Cloudflare 會自動重新部署，不用手動操作。

---

## 遇到問題時

**Actions 跑出紅色叉叉**
點進去看最下面的錯誤訊息，把訊息貼給我。最常見的是某個網站暫時擋住請求，重跑一次通常就好。

**某一季完全沒資料**
表示 AniList 那一季還沒建檔，或 `MIN_POPULARITY` 設太高。可以把 `config.mjs` 裡的數字調小。

**IMDb 分數大量缺失**
正常。動畫在 IMDb 的登錄方式跟日本番組不同，比對率約七成。缺失的作品程式會自動不計入權重，不會讓分數失真。

---

## 成本

| 項目 | 費用 |
|---|---|
| GitHub 公開倉庫 + Actions | 免費 |
| Cloudflare Pages | 免費（每月 500 次部署） |
| 五個評分站 API | 免費 |
| 域名 anirating.com | 約 NT$400 / 年（選用） |

**目前總支出：0 元。**
